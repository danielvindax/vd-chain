syntax = "proto3";
package vindax.assets;

import "gogoproto/gogo.proto";
import "vindax/assets/asset.proto";

option go_package = "github.com/danielvindax/vd-chain/protocol/x/assets/types";

// GenesisState defines the assets module's genesis state.
message GenesisState {
  repeated Asset assets = 1 [ (gogoproto.nullable) = false ];
}
